{
  "name": "core-mcp-bridges",
  "version": "0.1.0",
  "description": "Core hardware MCP bridges",
  "hardware_requirements": {
    "os": ["linux"],
    "arch": ["amd64"]
  },
  "hardware_dependencies": {
    "packages": [
      { "name": "alsa-utils", "manager": "apk", "stage": "runtime", "required": true },
      { "name": "fbv", "manager": "apk", "stage": "runtime", "required": true },
      { "name": "mpv", "manager": "apk", "stage": "runtime", "required": true },
      { "name": "iw", "manager": "apk", "stage": "runtime", "required": true },
      { "name": "wpa_supplicant", "manager": "apk", "stage": "runtime", "required": true },
      { "name": "dhcpcd", "manager": "apk", "stage": "runtime", "required": true }
    ]
  }
}
